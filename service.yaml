apiVersion: v1
kind: Service
metadata:
  name: corporatecompany-service
spec:
  type: NodePort
  selector:
    app: corporatecompany
  ports:
  - protocol: TCP
    port: 8086
    targetPort: 8086
    nodePort: 30086